import { StyleSheet } from "react-native";

const textStyle=(fontSize,fontFamily)=>StyleSheet.create({
    text:{
        fontSize:fontSize,
        fontFamily:fontFamily
    }
})

export default textStyle;